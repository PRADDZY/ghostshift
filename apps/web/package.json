{
  "name": "@ghostshift/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ghostshift:sync-shared": "pnpm --dir ../.. --filter @ghostshift/shared build",
    "build": "pnpm run ghostshift:sync-shared && pnpm exec vite build",
    "dev": "pnpm run ghostshift:sync-shared && pnpm exec vite",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@ghostshift/shared": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
